var creativePepTalk = new Podcast ('name', 'image', 'text', 'embed', 'link', 'intelligence', 'truthiness')
var creativePepTalk = new Podcast ('name', 'image', 'text', 'embed', 'link', 'intelligence', 'truthiness')


var podCastList = [creativePepTalk, Lore];

function Modal(name, image, text, embed, link) {
  this.name = name;
  this.image = image;
  this.text = text;
  this.embed = embed;
  this.link = link;
}

Modal.prototype.makeModal = function(podType) {
  if (podType === "intelligence")

  //function to show specifc modal content
}


function suggestion(podcast) {
  podCastList.forEach(function(podcast) {
    if (podcast.truthiness === "true") {

    }
  })

}



var newPodcast(name, image, text, )
